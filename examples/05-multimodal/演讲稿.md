# 模块 5｜多模态与宿主集成

这一章我们让 Agent 从“只读文本”进入“真实输入”。  
通过 `00-image-input.ts`，我们会把图片作为 prompt 附件输入，让 Agent 输出结构化观察。

你在讲解时可以强调三点：  
第一，多模态不是炫技，它是数据入口扩展。  
第二，输入类型变了，但 session、event、tool 的核心机制不变。  
第三，同一个 Agent 内核可以跑在不同宿主里，CLI 只是其中一种界面。

这一章结束时，观众应该能建立“内核与界面分离”的产品思维，为后续 Web 化做准备。
