# 模块 3｜会话、分支与恢复：把聊天变成工作流

这一章的主题是：让 Agent 对话从一次性输入输出，升级为可追溯的工程资产。  
我们会通过 `00-persistent-session.ts` 把会话落盘，演示如何恢复历史、延续上下文。

你在录制时可以这样讲：  
第一，持久化会话让我们能复盘“为什么得到这个结论”。  
第二，会话树和分支能力让我们能探索不同方案，而不是覆盖原有路径。  
第三，恢复机制让 Agent 真正具备“长期工作”的基础。

这一章结束后，观众应该理解：Agent 项目要走向生产，不是只看 prompt，而是要看 session lifecycle。
