# 模块 4｜扩展系统：给 Agent 装能力

这一章我们讲 Pi 的扩展力。  
重点不是“多写几个工具”，而是理解如何把能力做成可插拔模块。

我们会运行 `00-inline-extension.ts`，演示 extension 如何监听生命周期事件。  
你可以顺势讲清三层概念：  
Tool 是可调用能力，Skill 是任务知识模板，Extension 是行为与运行时扩展。  

录制时建议强调：  
真正可扩展系统的标志，不是功能数量，而是新增能力时不破坏现有系统。  
当你把能力封装清晰，后面做 DataAgent、办公自动化、可视化代理都能复用同一套底座。
